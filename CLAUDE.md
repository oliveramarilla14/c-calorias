# c-calorias

## Versioning

The frontend shows its version next to the "PLATO" logo, sourced from
`frontend/src/version.ts` (`APP_VERSION`). Bump this value before every
push with user-facing changes (patch bump for small fixes, minor for new
features), so the deployed app's displayed version always matches what's
live.
